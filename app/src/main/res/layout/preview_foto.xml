<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_gravity="center"
    android:padding="10dp"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="57dp"
        android:layout_marginBottom="10dp"
        android:fontFamily="@font/poppins"
        android:textColor="@color/black"
        android:text="Sebelum melanjutkan, pastikan posisi foto dalam posisi tegak, jika belum tegak klik icon rotate dibawah foto"
        tools:ignore="HardcodedText" />

    <ImageView
        android:id="@+id/alert_preview_camerax"
        android:layout_width="300dp"
        android:layout_height="286dp"
        android:layout_gravity="center" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/rotate_image_button"
        android:layout_width="50dp"
        android:layout_height="34dp"
        android:layout_gravity="center"
        android:layout_marginTop="20dp"
        android:background="@drawable/reload_button"
        app:icon="@drawable/ic_baseline_refresh_24"
        app:iconGravity="textStart"
        app:iconPadding="0dp" />
</LinearLayout>